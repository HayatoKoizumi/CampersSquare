<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-sm-10">
      <div class="text-center">
        <h2>投稿一覧</h2>
      </div>
      <%= render "admin/post_camps/index", post_camps: @post_camps %>
      <div class="d-flex justify-content-center mt-4">
        <%= paginate @post_camps %>
      </div>
    </div>
  </div>
</div>