<div class= 'container mt-5'>
  <div class='row'>
    <div class="col-md-7 mx-auto" >

      <h4 class="col-md-4 mx-auto headline mt-2 mb-4 text-center">ログイン</h4>

      <div class="p-3 rounded" style="background-color: #d8e5cb;">

        <div class='login-flash-message'>
          <%= flash[:alert] %>
        </div>

        <p class="text-center">会員の方はこちらからログイン</p>

        <%= form_with model: @user, url: session_path(resource_name), local: true do |f| %>
          <div class="form-group row mt-4 justify-content-center">
            <%= f.label :メールアドレス,class:"col-md-3 col-form-label" %>
            <div class="col-md-7">
              <%= f.email_field :email, autofocus: true, autocomplete: "email" ,class:"form-control" %>
            </div>
          </div>

          <div class="form-group row mt-4 justify-content-center">
            <%= f.label :パスワード,class:"col-md-3 col-form-label" %>
            <div class="col-md-7">
              <%= f.password_field :password, autocomplete: "current-password",class:"form-control" %>
            </div>
          </div>

          <div class="actions text-center mt-4 mb-2">
           <%= f.submit "ログイン", :class => "btn btn-lg mt-1", style: "background-color: #1a120b; color: #d8e5cb;" %>
          </div>
        <% end %>

      </div>

      <div class="text-center mt-5">
        <h4>会員登録がお済みでない方</h4>
        <h6><%= link_to "こちら", new_registration_path(resource_name) , class:"login-link" %>から新規登録を行なってください。</h6>
      </div>

    </div>
  </div>
</div>